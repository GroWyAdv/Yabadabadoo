/**
 * Returns the full location name based on the arguments. For example, "Turkey", "Canada (British
 * Columbia)", "US (Autauga, Alabama)".
 *
 * @param countryOrRegion
 * @param provinceOrState
 * @param county
 */
import { InternalLocationData } from "./types";
export declare function getFullLocationName(countryOrRegion: string, provinceOrState?: string, county?: string): string;
/**
 * Pushes an element into an array if the element doesn't already exist in the array.
 *
 * @param arr
 * @param el
 */
export declare function pushUnique<T>(arr: T[], el: T): void;
export declare function cloneInternalLocationData(data: InternalLocationData): InternalLocationData;
