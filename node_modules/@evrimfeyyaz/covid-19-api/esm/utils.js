import { __assign } from "tslib";
export function getFullLocationName(countryOrRegion, provinceOrState, county) {
    var location = countryOrRegion;
    var subLocation = provinceOrState;
    if (subLocation != null && county != null && subLocation.length > 0 && county.length > 0) {
        subLocation = county + ", " + subLocation;
    }
    if (subLocation != null && subLocation.length > 0) {
        location = location + " (" + subLocation + ")";
    }
    return location;
}
/**
 * Pushes an element into an array if the element doesn't already exist in the array.
 *
 * @param arr
 * @param el
 */
export function pushUnique(arr, el) {
    if (arr.indexOf(el) === -1) {
        arr.push(el);
    }
}
export function cloneInternalLocationData(data) {
    var clonedValues = data.values.map(function (valuesOnDate) { return (__assign({}, valuesOnDate)); });
    return __assign(__assign({}, data), { values: clonedValues });
}
//# sourceMappingURL=utils.js.map