import { DataGetter } from "./DataGetter";
/**
 * A data getter that loads the time series data from CSV files.
 *
 * *Returns `undefined` for the "source last updated at" date, as there is no way to determine this
 * without connecting to the GitHub repo.
 *
 * For more information about its methods see {@link DataGetter}.
 */
export declare class FileGetter implements DataGetter {
    private globalConfirmedCSVPath;
    private globalDeathsCSVPath;
    private globalRecoveredCSVPath;
    private usConfirmedCSVPath;
    private usDeathsCSVPath;
    constructor(globalConfirmedCSVPath: string, globalDeathsCSVPath: string, globalRecoveredCSVPath: string, usConfirmedCSVPath: string, usDeathsCSVPath: string);
    private static getFileAsString;
    getGlobalConfirmedData(): Promise<string>;
    getGlobalDeathsData(): Promise<string>;
    getGlobalRecoveredData(): Promise<string>;
    getUSConfirmedData(): Promise<string>;
    getUSDeathsData(): Promise<string>;
    getSourceLastUpdatedAt(): Promise<Date | undefined>;
}
