import { ParsedCSV } from "./parse";
import { InternalLocationData } from "./types";
/**
 * Converts parsed CSV objects that contain global COVID-19 time series into an easier to consume
 * format.
 *
 * @param parsedGlobalConfirmedData
 * @param parsedGlobalDeathsData
 * @param parsedGlobalRecoveredData
 */
export declare function formatGlobalParsedData(parsedGlobalConfirmedData: ParsedCSV, parsedGlobalDeathsData: ParsedCSV, parsedGlobalRecoveredData: ParsedCSV): InternalLocationData[];
/**
 * Converts parsed CSV objects that contain US COVID-19 time series into an easier to consume
 * format.
 *
 * @param parsedUSConfirmedData
 * @param parsedUsDeathsData
 */
export declare function formatUSParsedData(parsedUSConfirmedData: ParsedCSV, parsedUsDeathsData: ParsedCSV): InternalLocationData[];
