"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.cloneInternalLocationData = exports.pushUnique = exports.getFullLocationName = void 0;
var tslib_1 = require("tslib");
function getFullLocationName(countryOrRegion, provinceOrState, county) {
    var location = countryOrRegion;
    var subLocation = provinceOrState;
    if (subLocation != null && county != null && subLocation.length > 0 && county.length > 0) {
        subLocation = county + ", " + subLocation;
    }
    if (subLocation != null && subLocation.length > 0) {
        location = location + " (" + subLocation + ")";
    }
    return location;
}
exports.getFullLocationName = getFullLocationName;
/**
 * Pushes an element into an array if the element doesn't already exist in the array.
 *
 * @param arr
 * @param el
 */
function pushUnique(arr, el) {
    if (arr.indexOf(el) === -1) {
        arr.push(el);
    }
}
exports.pushUnique = pushUnique;
function cloneInternalLocationData(data) {
    var clonedValues = data.values.map(function (valuesOnDate) { return (tslib_1.__assign({}, valuesOnDate)); });
    return tslib_1.__assign(tslib_1.__assign({}, data), { values: clonedValues });
}
exports.cloneInternalLocationData = cloneInternalLocationData;
//# sourceMappingURL=utils.js.map